@model IEnumerable<SetorViewModel>

<div class="row">
    <div class="col-md-12 form-group">
        <h2>Lista de Setores</h2>
    </div>
</div>

<vc:botao-default controller-name="Setor" tipo-tela="Listar"></vc:botao-default>
<vc:summary />

<div class="row">
    <div class="col-md-12 form-group table-responsive">
        <table class="table table-bordered table-hover">
            <tr>
                <th>
                    @LabelResource.LBL_CODIGO
                </th>
                <th>
                    @LabelResource.LBL_NOME
                </th>
                <th>
                    @LabelResource.LBL_SIGLA
                </th>
                <th>
                    @LabelResource.LBL_ACOES
                </th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Codigo
                    </td>
                    <td>
                        @item.Nome
                    </td>
                    <td>
                        @item.Sigla
                    </td>
                    <td>
                        <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Codigo">Editar</a>
                        <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Codigo">Excluir</a>
                    </td>
                </tr>
            }
        </table>
    </div>
</div>





